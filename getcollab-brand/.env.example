# Local API. localhost is rewritten for Android emulators / devices in dev
# builds; set EXPO_PUBLIC_USE_LOCALHOST=true only when using adb reverse.
EXPO_PUBLIC_API_URL=http://localhost:4000/api/v1

EXPO_PUBLIC_APP_ENV=development

# Crash reporting (leave empty locally)
EXPO_PUBLIC_SENTRY_DSN=
